`npx react-native upgrade`
`yarn upgrade`
// `yarn upgrade` doesn't update a major version. It will upgrade from 15 to 15.6 something but not to 16.0 for that you should use `yarn upgrade --latest`
`rm -rf node_modules`
`yarn install`

ios
(in ios folder) `rm -rf Pods && rm Podfile.lock`
(in ios folder) `pod install --repo-update`
`rm -rf ~/Library/Developer/Xcode/DerivedData/`
